kind: Deployment
apiVersion: apps/v1
metadata:
  name: sample-nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sample-nodejs
  template:
    metadata:
      labels:
        app: sample-nodejs
    spec:
      containers:
        - name: sample-nodejs
          image: nodejs-image:latest
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
          resources:
            limits:
              memory: "1024Mi"
              cpu: "500m"
---
kind: Service
apiVersion: v1
metadata:
  name: sample-nodejs-svc
spec:
ports:
  - name: http-3002
    port: 3001
    protocol: TCP
    targetPort: 3001
selector:
  app: sample-nodejs